# 群组服对接需求

**面向**：负责搭建 Velocity 群组服的同学
**目的**：让 QQ 群机器人能查询各子服的在线玩家，并在群里管理白名单

> 文档里每条要求都附了原因。原因没消失之前请不要"简化掉"某项配置 —— 大部分是我们踩过或查过的坑。

**请先区分两类要求**，方便排优先级：

| 标记 | 含义 |
|---|---|
| **【群组自身】** | 群组本身要跑起来就必须做的，**和机器人无关** |
| **【为了机器人】** | 为了让机器人能查询玩家、管理白名单才需要加的 |

缺了【群组自身】的东西，群组根本跑不起来；缺了【为了机器人】的东西，群组正常但机器人查不到数据。

---

## 0. 整体目标

机器人需要做到三件事：

1. 在 QQ 群里显示**每个子服**的在线人数和玩家名单
2. 在 QQ 群里执行白名单的**添加 / 移除 / 查看**
3. 服务器挂掉或恢复时在群里提醒

为此需要**两条互相独立的通道**：一条读（拿名单），一条写（改白名单）。

## 1. 数据流

```
                     ┌── 代理 SLP (TCP 25565) ──→ 全群组总人数
                     │
QQ 群 ── 机器人 ─────┼── 各子服 SLP ──────────→ 各子服在线人数
                     │
                     ├── 各子服 RCON ─────────→ 各子服玩家名单  （只读）
                     │
                     └── 代理 RCON ───────────→ 白名单命令      （写）
                          (Velocircon)
```

**分服名单走各子服的 RCON，不走代理。** 因为 Velocity 和 BungeeCord 不一样，**没有原生 `glist`**，代理层拿不到"谁在哪个子服"。所以**每个子服的 RCON 都必须开** —— 这是本文档最核心的要求。

> **代理上请不要装 glist 类的插件。** 有人会顺手装一个来补 Velocity 没有 `glist` 的空缺，这份文档**不需要它**：
>
> - 它的输出是给玩家看的**人读文本**，插件可以整个改写、也可能跟随客户端语言本地化，机器人没法稳定解析；
> - 更实际的是，这类插件版本参差，我们没有条件逐版本验证。
>
> 走各子服的 RCON 就没有这个问题：`list` 的输出虽然同样是文案，但有「**名单条数 == SLP 报的人数**」这条独立交叉校验兜底，对不上就判为不完整并降级，不会拿着残缺名单乱报。
>
> 顺带一个好处：代理那台挂了，也不该让所有子服的名单一起消失。

### 为什么既要 SLP 又要 RCON

**SLP（Server List Ping）**就是游戏客户端"添加服务器"时用的那个协议，负责显示 MOTD、在线人数、延迟、版本号。机器人用它判断服务器**是否存活**，并拿到**准确的在线人数**。

**RCON** 用来拿**玩家名单**。

之所以要两个通道，是因为它们各有各的短板：

| | 在线人数 | 玩家名单 |
|---|---|---|
| **SLP** | ✅ 权威来源 | ❌ 给不了 |
| **RCON** | ❌ 不可靠 | ✅ 权威来源 |

- **人数不能从 RCON 拿**：`list` 的输出是给玩家看的**本地化文案**，中英文格式完全不同，插件还能整个改写，从文案里抠数字太脆弱。
- **名单不能从 SLP 拿**：SLP 返回的玩家样本默认**最多只有 12 个随机玩家**，服务端可以主动隐藏，插件还能往里塞广告。

所以方案是：**人数信 SLP，名单信 RCON，再用"名单条数 == 人数"交叉校验**。对不上就判定名单不完整并降级，而不是拿着残缺名单瞎报。

**结论：两个通道都必须通。**

---

## 2. 代理侧配置 —— 【群组自身】

### 2.1 转发（每个子服都要做）—— 【群组自身，与机器人无关】

使用 **现代转发（modern forwarding）**：`velocity.toml` 里 `player-info-forwarding-mode = "modern"`，并设置 `forwarding.secret`。

然后 **每一个 Fabric 子服**都要安装，一个都不能漏：

1. **Fabric API** —— FabricProxy-Lite 的前置依赖
2. **FabricProxy-Lite** —— 它只支持 Velocity 现代转发

每个子服的 `config/FabricProxy-Lite.toml` 里的 `secret`，都必须与代理的 `forwarding.secret` **完全一致**。

> **为什么是"每个子服"而不是装一台就够？**
>
> 转发握手是**后端各自完成**的：每个后端都要能读懂代理转发过来的玩家信息（UUID / IP / 皮肤），并自己校验 secret。没有中心节点可以代劳。
>
> **漏装一个的后果**：玩家一跳那个子服就掉线。以后新增子服同样要装（见 §7.3）。

> **注意**：Fabric 服务端**没有** `spigot.yml`，Spigot 系那套 `settings.bungeecord: true` 开关在 Fabric 上不存在，**必须靠 mod** —— 不要按 Spigot 的经验去找这个配置项。

> 原因：现代转发比 legacy（BungeeCord 转发）安全，且是 Velocity 官方对 Fabric 后端的推荐组合。

### 2.2 `ping-passthrough` 保持 `DISABLED` —— 【群组自身 + 机器人共用】

`velocity.toml` 中 `ping-passthrough` 决定代理的 SLP 响应是否交给后端代答：

| 取值 | 后果 |
|---|---|
| `DISABLED`（默认，**请保持**）| 代理自己答，机器人拿到的是**全群组总人数** ✅ |
| `MODS` / `DESCRIPTION` / `ALL` | ping 返回的是**某一个后端**的响应，机器人会把单个子服的人数当成总人数 ❌ |

> 原因：这个值被改后机器人**不会报错**，只会默默显示错误的数字，非常难排查。除非代理上装了 MOTD 插件（那种情况才需要 `ALL`），否则请保持默认。

---

## 3. 代理 RCON（白名单通道）—— 【为了机器人】

Velocity 没有原生 RCON，需要安装 **Velocircon**（[GitHub](https://github.com/code-lime/Velocircon) / [Hangar](https://hangar.papermc.io/code-lime/velocircon)）。

配置文件 `plugins/velocircon/rcon.yml`，**以下是默认值，其中三项必须确认**：

| 配置项 | 默认值 | 要求 |
|---|---|---|
| `enable` | **`false`** | **必须改成 `true`** —— 否则插件根本不启动 |
| `host` | **`0.0.0.0`** | 监听所有网卡。**必须用防火墙限制来源**（见 §7.1）|
| `port` | `25575` | 告知我们实际值 |
| `password` | `PASSWORD` | 设置强密码并告知 |
| `permissions` | 有 LuckPerms 组 / 正则过滤 | **确认放行机器人要用的白名单命令**，或留空不作限制 |
| `console-output` | `true` | **保持开启** —— 机器人靠它拿到命令输出 |

> `permissions` 这条容易被忽略：如果配了限制而没放行白名单命令，现象是"命令发过去毫无反应"，很难查。

---

## 4. 代理层白名单插件 —— 【群组自身 + 机器人共用】

> ### ⚠️ 先分清两个「list」
>
> 本文档里 `list` 出现两次，**同名不同义**，看到先确认是谁的命令：
>
> | 命令 | 出处 | 含义 |
> |---|---|---|
> | `list` | **原版 MC 服务端**（各子服 RCON）| **在线玩家** —— 现在谁在服里 |
> | `list` | **白名单插件**，如 Global Whitelist 的 `/globalwhitelist list` | **白名单** —— 谁被允许进来 |
>
> 混起来会把「白名单里有 3 个人」读成「服里有 3 个人在线」——两者毫无关系。
> **本章说的 `list` 一律指白名单插件自己的命令**，与在线玩家无关；在线玩家那边走的是 §1 里各子服的 RCON。

### 4.1 必须带 `list` 命令（硬要求）

机器人判定"添加成功没有"的方式是：**执行完 `add` 之后再跑一次 `list` 反查**，而不是读命令回执（回执文案会随语言/版本变，不可靠）。

所以：

- ❌ **没有 `list` 命令的插件一律不能用** —— 机器人将无法判断成败
- ✅ `list` 的输出**必须包含玩家名**，不能只回一条数字（如"There are 3 players"）

### 4.2 候选插件

| 插件 | 命令 | 有 `list`？ |
|---|---|---|
| **Global Whitelist**（推荐）| `/globalwhitelist add\|remove\|list` | ✅ 且名单直接用 vanilla `whitelist.json` 格式，便于迁移 |
| ProxyWhitelist | `/pwl set\|remove\|list\|get` | ✅ |
| VelocitySafe | `/vwhitelist add\|remove\|list` | ✅ |
| NekoList | `/whitelist add\|remove\|list` | ✅ |
| VelocityWhitelist (james090500) | `/vwhitelist add\|remove\|on\|off\|reload` | ❌ **没有 list，不要用** |

推荐 **Global Whitelist**：命令集刚好是需要的三个动词，且名单格式与 vanilla 一致。

### 4.3 命令前缀要单独告诉我们

机器人是**通过 RCON 发命令**的，它发出去的那串字符必须和插件注册的命令**完全一致**，差一个字母就是 `Unknown command`。

而候选插件的命令前缀**并不都叫 `whitelist`**：

| 插件 | RCON 里实际要发的命令前缀 |
|---|---|
| **Global Whitelist** | `globalwhitelist` ← 注意：**没有** `whitelist` 这个名字 |
| NekoList | `whitelist` |
| ProxyWhitelist | `pwl` |
| VelocitySafe | `vwhitelist` |

所以 §6.1 那张表请**逐个动词写完整命令**，不要只写「白名单命令」四个字。

> **触发词和命令前缀是两个不同的轴，别混：**
>
> - **触发词**：QQ 群里 @机器人 时打的词（默认 `whitelist`），由**我们这边**配置，对方不用管；
> - **命令前缀**：机器人往 RCON 里发的命令（如 `globalwhitelist`），**必须和插件实际命令一致**，这是要对方告知的。
>
> 两个词今天恰好同名，但**允许不一样**——群里打 `whitelist`、RCON 里发 `globalwhitelist`，是完全正常的组合，不需要为了迁就机器人去改插件命令。

### 4.4 各子服的 `white-list` 保持 `false`

代理层白名单插件存的是**它自己的名单文件**，不是后端的 `server.properties`。

> 原因：如果后端也开 `white-list=true`，会出现"代理放行了但后端又拦一遍"，而且两份名单迟早不一致。

---

## 5. 各子服配置 —— 【混合，见下表"用途"列】

**每个子服**的 `server.properties`：

| 配置项 | 要求 | 用途 |
|---|---|---|
| `enable-rcon` | `true` | 【为了机器人】|
| `rcon.port` | 每台**各不相同**（同机会撞）| 【为了机器人】|
| `rcon.password` | 强密码，**不要**和 `server-port` 相同 | 【为了机器人】|
| `broadcast-rcon-to-ops` | **`false`** ← 见下方警告 | 【为了机器人】防止刷屏 |
| `online-mode` | `false`（现代转发要求）| 【群组自身】|
| `white-list` | `false`（白名单在代理层）| 【群组自身】|

> ⚠️ **`broadcast-rcon-to-ops` 目前是 `true`**（bingo 和 backstabbed 都是）。机器人会按固定间隔查询玩家名单，设为 `true` 会把每次查询结果**广播给所有 OP**，游戏内聊天框持续刷屏。**必须改。**

### 游戏端口也要对机器人可达 —— 【为了机器人】

除了 RCON，机器人还要通过子服的**正常游戏端口**做一次 SLP 探测（见 §1），用来确认该子服存活并拿到准确人数。

所以子服的 `server-port` 同样要对机器人可达。群组和机器人通常在同一台机器上，绑 `127.0.0.1` 即可，**不需要**对外暴露。

> 直连子服端口做 SLP 探测是正常的：转发只影响玩家的登录握手，不影响状态查询。

### 子服名保持一致

子服名请与 `velocity.toml` 的 `[servers]` 段**完全一致**。

> 原因：机器人显示的服名和玩家在游戏内用 `/server` 看到的名称要对得上，否则沟通时会对不上号。

### 子服名不要和机器人的触发词撞词

机器人判断"这条消息是在问哪个功能"是靠**在整句话里做子串匹配**，而且 `whitelist` / `oopz` 的优先级**高于** `mc`。

所以取名叫 `whitelist`、`oopz`（或含这两个词）的子服，**永远当不了查询参数**——那条消息会被机器人当成白名单管理或 oopz 查询吃掉，查询功能根本不接手。

**取名时避开**：`whitelist`、`oopz`，以及我们这边 `MC_TRIGGER` 里配的词（默认 `mc` / `我的世界` / `服务器`）。

> 这条只能靠取名避开，没有别的解法。如果确实需要一个这样的名字，告诉我们，我们给它加个别名（机器人支持一个子服配多个叫法），但**正式名字**仍然建议避开。

---

## 6. 需要告知我们的信息

### 6.1 代理

| 项目 | 值 |
|---|---|
| 代理地址 : 端口 | |
| 代理 RCON 端口 | |
| 代理 RCON 密码 | |
| 白名单插件名 | |
| **RCON 里实际发的命令前缀**（见 §4.3，不是群里打的触发词）| |
| `ping-passthrough` 取值 | |
| 转发模式 | |

**白名单三条命令请逐条写完整**（前缀按上面填，别默认是 `whitelist`）：

| 动作 | RCON 里发什么（完整命令，含参数占位）| 示例：加一个叫 `Steve` 的玩家 |
|---|---|---|
| 添加 | | |
| 移除 | | |
| 查看名单 | | |

**再附一段「查看名单」命令的真实输出**——直接从 RCON 粘过来即可，**原样、不要整理格式**：

```
（粘贴在这里）
```

> **为什么要原始输出**：机器人判读名单是靠**文本格式**（它按第一个冒号切）。如果实际输出里**没有冒号**，机器人会判为"格式不认识"，于是所有白名单变更都只能回「未能验证」。
> 这不是致命问题（它会老实说不知道，而不是瞎报成功），但能看到真实格式我们就能一次把解析器配对。
>
> ⚠️ **不要**贴「在线玩家」那条 `list` 的输出——那是 §4 开头说的另一个 `list`，我们要的是**白名单**这条。

### 6.2 子服列表

| 子服名（与 `velocity.toml` 一致）| 地址 : 端口（给玩家/机器人查询用）| RCON 端口 | RCON 密码 |
|---|---|---|---|
| | | | |
| | | | |
| | | | |
| | | | |

> 如果还有 limbo / 登录服这类**非游玩子服**，请一并列出并注明。

---

## 7. 注意事项

### 7.1 RCON 是明文协议，必须限制来源 —— 【为了机器人】

RCON 走明文、密码可被重放，而 **原版 MC 的 RCON 和 Velocircon 都无法绑定指定网卡**（默认监听所有接口）。

**要求**：用防火墙 / 安全组把 RCON 端口限制为**只允许机器人所在的机器访问**。这不是可选项。

### 7.2 独立模组服不接群组

GTNH 等需要专用客户端的独立服务器**不接入群组**（版本不同、客户端不通用）。它们的查询与白名单单独配置，不在本文档范围内。

### 7.3 新增子服时的配合

新增子服需要：

1. 按 §2.1 装好 **Fabric API + FabricProxy-Lite**（`secret` 与代理一致）
2. 按 §5 配置其 `server.properties`
3. 把它的名字、端口、RCON 信息**同步给我们**（否则机器人不认识它）
4. 白名单在代理层，所以**不需要**单独维护 —— 这是代理层方案的优点

### 7.4 版本兼容

FabricProxy-Lite 这类 mod 的更新通常滞后于 MC 版本。请确认所用版本**支持你们的目标版本**，不要假设。

---

## 8. 验收方式

搭建完成后，我们会按 §1 的数据流**逐个目标**验证。不同角色的目标，验收标准不一样：

| 目标 | 验什么 | 通过标准 |
|---|---|---|
| **代理** | SLP 可达 | 返回**全群组总人数**（不是某一个子服的人数——见 §2.2）|
| **代理** | RCON 能执行白名单三条命令 | `add` / `remove` / **白名单 `list`** 都通，且 `list` 的输出我们能解析 |
| **每个子服** | SLP 可达 | 能连上，报的人数与实际在线一致 |
| **每个子服** | RCON 能取**在线玩家**名单 | 名单**条数**等于 SLP 报的人数（对不上我们会判为不完整并降级）|

两点措辞说明，避免验收时对不上：

- **代理那条 RCON 验的是白名单 `list`**，子服那条 RCON 验的是**在线玩家** `list`。同名的两个命令，验的是两回事（见 §4 开头）。
- **代理不验在线名单**：Velocity 拿不到"谁在哪个子服"，代理的 SLP 只出**总人数**。分服名单逐个子服验。

验收通过后，群里能查到的就是：**每个子服各自的人数 + 各自在线的玩家名单 + 全群组总人数**。

如果某项不通，我们会带着具体报错回来找你对。
